[project]
name = "autoheal-proxy"
version = "0.1.0"
description = "Zero-downtime MCP proxy with persistent caching"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
    "pydantic",
    "redis",
    "httpx",
    "fakeredis>=2.37.0",
]
